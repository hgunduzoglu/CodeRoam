name: coderoam_noise_ffi
description: Internal CodeRoam Noise native-core bindings.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  code_assets: ^1.0.0
  hooks: ^1.0.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.28.0
